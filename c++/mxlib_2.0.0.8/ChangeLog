1 mxlib_2.0.0.6-[20120425]:

INFO:
	创建
ADD:

MODIFY:

FIX:：

TODOS:

2 mxlib_2.0.0.7-[20120425]:

INFO:
	
ADD:

MODIFY:
	把HttpListening中virtual server容器由map改为list, 并把匹配改为大小写不敏感
	在根据host找不到virtual server时使用第一个list中的第一个virtual server
FIX:

TODOS: